# ================================================================
# GENERIC SESSION DEBRIEF — MotoPass
# Generated by Standard Project Kit (skill standard-project-kit)
# ================================================================

runbook:
  name: Session Debrief
  version: "2.0"
  trigger:
    phrases: ["run session debrief"]

project_config:
  project_name: "MotoPass"
  context_file: null
  framework_terms: null
  id_prefixes:
    architectural_decision: "AD-"
    open_question: "OQ-"
  output_location: "docs/debriefs/"

output_schema:
  session_id: "session-2026-08-19-001"
  date: "2026-08-19"
  status: "confirmed"

  summary:
    purpose: "Adopt the Standard Project Kit on MotoPass and re-sync the THOR copy after a fresh git pull."
    outcomes:
      - "THOR copy /root/ref/motopass pulled fresh from origin — now on 440198b (BUILD 71 session-end handoff), 13 commits caught up."
      - "Verified live: motopass.giveabit.io returns HTTP 200 with correct title; site is up on BUILD 71."
      - "Recorded the first machine-readable session debrief for MotoPass."
      - "Confirmed MotoPass is already present in MASTER-BRAIN (03-Projects/motopass), closing the handoff's 'integrate into MASTER-BRAIN/Kanban' next-step on the vault side."

  architectural_decisions:
    - id: "AD-001"
      decision: "Treat /root/ref/motopass on THOR as the live/reference + handoff home. M3/M4 clones are working copies synced via git push/pull only."
      rationale: "Consistent with the Standard Project Kit rule established for SherpaCarta (AD-001 there). Single source of truth on the ops brain; no copying folders between machines."
      status: "agreed"

  open_questions:
    - id: "OQ-001"
      question: "Should a MotoPass session debrief also be created for the 2026-08-18 BUILD 71 work that happened on M3 (Grok), or only for THOR-side sessions?"
      context: "BUILD 68-71 all shipped from Grok on M3, recorded in the handoff but with no machine-readable debrief. The debrief pipeline runs at THOR session end."
      priority: "medium"
      status: "open"

    - id: "OQ-002"
      question: "Is the LNbits BOLT11 mint blocked on a THOR node-env readiness decision, or is it simply not started yet?"
      context: "Handoff lists 'LNbits BOLT11 mint (when node env is ready)' as next work, referencing VITE_LNBITS_* vars. This gates the full Lightning invoicing path."
      priority: "medium"
      status: "open"

  session_notes:
    deprecated_items: []
    corrections_to_prior_sessions: []
    other:
      - "Trial run of the Standard Project Kit on MotoPass. First debrief for this repo; prior sessions recorded only in KIMI-HANDOFF.md prose."
