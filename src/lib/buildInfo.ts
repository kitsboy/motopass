/** Single source of truth for BUILD — bump BUILD_ID on every push/ship */
export const BUILD_ID = '2026.08.20-72'
export const BUILD_LABEL = `Eager remaining lazy routes (BTC Map / Portfolio / Blog), hero e2e selector fix`
export const BUILD_DATE = '2026-08-20'
/** Shown in site footer — must stay in sync with BUILD_ID */
export const FOOTER_VERSION = `BUILD ${BUILD_ID}`