/** Single source of truth for BUILD — bump BUILD_ID on every push/ship */
export const BUILD_ID = '2026.08.18-70'
export const BUILD_LABEL = `Fix programs crash: dedupe react-router, eager programs/vault, route error boundary`
export const BUILD_DATE = '2026-08-18'
/** Shown in site footer — must stay in sync with BUILD_ID */
export const FOOTER_VERSION = `BUILD ${BUILD_ID}`