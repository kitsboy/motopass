# Cloudflare Pages — explicit static paths BEFORE SPA fallback (prevents index.html caching as .js)
/assets/*    /assets/:splat    200
/website/*   /website/:splat   200
/research/*  /research/:splat  200
/images/*    /images/:splat    200
/*           /index.html       200
